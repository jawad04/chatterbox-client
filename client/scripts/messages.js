var Messages = function (userName, message, roomName) { //message class
  this.userName = userName;
  this.message = message;
  this.roomName = roomName;
};

Messages.prototype.render = function() {
  
};
